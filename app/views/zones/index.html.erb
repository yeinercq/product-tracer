<div class="container">
  <div class="header">
    <h1>Zonas de produccion de cafe</h1>
    <%= link_to "Nueva zona", new_zone_path, class: "btn btn--primary" %>
  </div>

  <%= render @zones %>
</div>
